package com.taskmanagement.model;

/**
 * Enum representing the status of a task in the system.
 * Used for filtering and updating task progress.
 */
public enum Status {
    TODO,
    IN_PROGRESS,
    DONE
}
